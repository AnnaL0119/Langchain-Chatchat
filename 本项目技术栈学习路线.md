# 本项目技术栈学习路线

> 面向对象：大数据专业本科阶段，想以本项目为载体系统学习大模型应用开发的同学。
> 学习原则：**以项目真实代码为教材**，每阶段都对应本项目中的具体文件，学完即可看懂/修改对应模块。

---

## 总览：本项目技术栈地图

```
┌─────────────────────────────────────────────────────────┐
│  用户浏览器                                              │
│    └─ Streamlit WebUI（前端）                            │
│         webui.py / webui_pages/*  ← 阶段1~3 的改造都在这  │
├─────────────────────────────────────────────────────────┤
│  FastAPI 后端（API Server, 端口7861）                     │
│    chat_routes / kb_routes / server_routes               │
├──────────────┬──────────────────────┬───────────────────┤
│  Langchain   │  向量数据库(FAISS)    │  Agent/MCP 工具    │
│  对话编排/检索 │  知识库RAG检索        │  联网/计算/搜索等  │
├──────────────┴──────────────────────┴───────────────────┤
│  Ollama 本地模型服务（端口11434）                         │
│    LLM: qwen2:1.5b   Embedding: bge-large-zh-v1.5        │
└─────────────────────────────────────────────────────────┘
```

学习顺序（也是依赖顺序）：

| 阶段 | 技术栈 | 优先级 | 预计投入 |
|------|--------|--------|---------|
| 1 | Python 基础 | ⭐⭐⭐ 必学 | 2~4 周 |
| 2 | Streamlit 前端 | ⭐⭐⭐ 必学 | 1~2 周 |
| 3 | Langchain | ⭐⭐⭐ 核心 | 2~3 周 |
| 4 | 向量数据库与 RAG | ⭐⭐ 重点 | 2 周 |
| 5 | 大模型调用（OpenAI 协议） | ⭐⭐ 重点 | 1 周 |
| 6 | Ollama 本地部署 | ⭐ 入门即可 | 3~5 天 |

---

## 阶段 1：Python 基础（⭐⭐⭐ 一切的前提）

**学习重点**

1. 基础语法：变量、流程控制、函数、异常处理（`try/except`）
2. 数据结构：`list` / `dict` / `tuple` / `set` 的增删改查与推导式
3. 面向对象：`class`、`__init__`、属性与方法（看懂项目里 `ApiRequest` 类即可）
4. 模块与包：`import`、`if __name__ == "__main__"`、包的目录结构
5. 类型注解与装饰器：`typing.List/Dict/Optional`、`@装饰器`（Langchain 全是这两个）
6. 文件与编码：`open()`、UTF-8 编码（中文项目必踩的坑）

**结合项目练习**

- 练习1：阅读 `libs/chatchat-server/chatchat/webui_pages/utils.py` 中的 `ApiRequest` 类，画出它的方法清单，理解"类封装 HTTP 请求"的写法。
- 练习2：给 `upload_utils.py` 的 `format_file_size()` 写 5 个测试用例，理解函数输入/输出边界。
- 练习3：模仿 `webui_pages/utils.py` 的 `check_error_msg()`，自己写一个 `check_success_msg` 的变体函数。

**推荐资料**：Python 官方中文教程（docs.python.org/zh-cn/3/tutorial）、B 站"Python 面向对象"专题。

---

## 阶段 2：Streamlit 前端（⭐⭐⭐ 本项目前端骨架）

**为什么学**：Streamlit 让你只用 Python 就能写 Web 界面，是数据/大模型应用最流行的快速开发框架。本项目阶段1~3 的所有改造（主题、上传校验、会话管理、导出）都在这一层。

**学习重点**

1. 运行模型：`st.rerun()` 重跑机制——**每次交互整个脚本从头执行一遍**，这是理解一切 Streamlit 行为的钥匙
2. 会话状态：`st.session_state` 字典（跨重运行保存数据）
3. 基础组件：`st.button / st.text_input / st.file_uploader / st.selectbox / st.radio`
4. 布局组件：`st.sidebar / st.tabs / st.columns / st.expander`、`st.dialog` 弹窗
5. 反馈组件：`st.error / st.success / st.toast / st.spinner`
6. 进阶组件（项目在用）：
   - `streamlit_chatbox`：本项目对话页的核心（`ChatBox` 的 history/context/会话管理）
   - `streamlit-antd-components (sac)`：侧边栏菜单、会话切换按钮
   - `streamlit_extras.bottom_container`：把输入框固定在底部
7. 配置与主题：`.streamlit/config.toml`（端口、上传上限、主题色）

**结合项目练习**

- 练习1：本地跑一个 10 行的 Streamlit demo（计数器按钮），体会 `st.rerun`。
- 练习2：阅读 `webui_pages/dialogue/dialogue.py` 的 `add_conv / del_conv / batch_del_convs` 三个函数，对照界面上"新建/删除/批量删除"按钮，理解"按钮点击 → 全脚本重跑 → 函数执行"的过程。
- 练习3：给"会话设置"标签页加一个"复制会话"按钮（提示：用 `chat_box.get_chat_names()` + `use_chat_name()`）。
- 练习4：阅读 `upload_utils.py`，体会"上传前的前端校验"如何通过 `st.error` 给出中文提示。

**推荐资料**：Streamlit 官方文档（docs.streamlit.io）的 Get Started + Cheat sheet；搜索"streamlit chatbot tutorial"。

---

## 阶段 3：Langchain（⭐⭐⭐ 大模型应用编排框架）

**为什么学**：Langchain 把"提示词→模型→解析→记忆→工具→检索"串成流水线，是本项目后端的骨架。

**学习重点**

1. 核心抽象：`LLM/ChatModel`（模型封装）、`PromptTemplate`（提示词模板）、`OutputParser`（输出解析）
2. 链式调用：`LLMChain` 与 LCEL 管道写法（`|` 操作符）
3. 记忆机制：`ConversationBufferMemory` 等（对应对话历史 history_len）
4. Document 与 Loader：`DocumentLoader → TextSplitter → Embedding → VectorStore` 四件套（RAG 数据流水线）
5. Agent 与工具：`Tool`、`AgentExecutor`（对应项目 `server/agent/tools_factory/` 下的联网搜索、计算器、Wikipedia 等工具）
6. Callbacks：回调机制（项目 `agent_callback_handler.py` 用它实现"正在调用工具..."的流式状态展示）

**结合项目练习**

- 练习1：阅读 `server/chat/chat.py`，找到"拼接 history → 调用模型 → 流式返回"的主干代码。
- 练习2：阅读 `server/agent/tools_factory/calculate.py` 和 `search_internet.py`，理解一个"工具"的定义结构（name/description/args/func）。
- 练习3：仿写一个自定义工具（如"当前时间查询"），在 WebUI 的工具设置里启用它并对话测试。
- 练习4：阅读 `server/file_rag/text_splitter/chinese_recursive_text_splitter.py`，理解中文分块策略为什么要按 `\n` 和句号切分。

**推荐资料**：Langchain 官方文档（python.langchain.com）；吴恩达×Langchain 联名公开课《LangChain for LLM Application Development》（B站有中文字幕版）。

---

## 阶段 4：向量数据库与 RAG 检索（⭐⭐ 知识库功能的底层）

**为什么学**：RAG（检索增强生成）是大数据专业同学最该关注的落地范式——本质是"非结构化数据的 ETL + 相似度检索 + 生成"。

**学习重点**

1. Embedding 概念：文本 → 向量（一串浮点数），语义相近则向量距离近
2. 相似度检索：余弦相似度 / 内积 / L2 距离，Top-K 检索
3. FAISS 入门：本项目默认向量库（`kb_settings.yaml` 中 `DEFAULT_VS_TYPE: faiss`），本地零部署
4. 检索参数：`top_k`（召回条数）、`score_threshold`（相似度阈值）——就是 WebUI 里"匹配知识条数/分数阈值"两个滑块
5. 混合检索与重排：`ensemble.py`（BM25 + 向量混合）、rerank 模型
6. 对比了解：Milvus（项目也支持 `milvus_vectorstore.py`）、Chroma 等其他向量库的适用场景

**结合项目练习**

- 练习1：在 WebUI"知识库管理"里新建知识库，上传 1~2 个 md/pdf，观察 `data/knowledge_base` 目录里生成了什么。
- 练习2：调 `python -c "import faiss"` 后写 10 行 demo：建索引 → 存 3 条向量 → 检索最相近的一条。
- 练习3：在 RAG 对话页把 `top_k` 从 3 调到 1、阈值从 0 调到 2，观察回答引用的变化，理解参数含义。
- 练习4（进阶）：阅读 `server/knowledge_base/kb_service/base.py`，梳理"上传文件→分块→向量化→入库"的完整调用链。

**推荐资料**：FAISS 官方 Get Started；B 站搜索"RAG 从零实现"；大数据课程中的"相似性检索/ANN 索引"章节可以对照 HNSW/IVF 索引原理。

---

## 阶段 5：大模型调用与 OpenAI 协议（⭐⭐ 一切模型的统一入口）

**为什么学**：OpenAI 的 `/v1/chat/completions` 协议已成为行业事实标准——本项目 WebUI 直接用 `openai.Client` 调自己的后端，Ollama 也暴露同样的协议。

**学习重点**

1. Chat Completions 协议：`messages=[{"role": "user/system/assistant", "content": ...}]`、`temperature`、`max_tokens`
2. 流式输出：`stream=True` 时的 SSE 逐 token 返回（对应 WebUI"打字机效果"）
3. Function Calling / Tools：让模型输出结构化的"工具调用请求"（Agent 的底层机制）
4. 多模态消息：`content` 里混合 `image_url`（对应多功能对话页的图片对话）
5. 平台概念：base_url + api_key 即可切换任何兼容服务商（本地 Ollama、云上 API 均同协议）

**结合项目练习**

- 练习1：阅读 `dialogue.py` 中 `client = openai.Client(base_url=f"{api_address()}/chat", ...)` 一段，画出"前端→后端→Ollama"的请求路径。
- 练习2：用 Python 直接对 `http://127.0.0.1:11434/v1/chat/completions` 发起一次流式请求，打印每个 chunk。
- 练习3：给对话加 `temperature=0` 和 `temperature=1` 各跑一次，对比输出稳定性。

**推荐资料**：OpenAI API 官方文档（platform.openai.com/docs）；Ollama OpenAI 兼容文档。

---

## 阶段 6：Ollama 本地部署（⭐ 一天上手，本地跑模型）

**为什么学**：不花钱、不断网就能跑大模型，是学习与毕设开发的最佳底座。

**学习重点**

1. 核心命令：`ollama pull / run / list / rm`、模型名格式（`qwen2:1.5b` = 模型:参数量）
2. 量化概念：GGUF 格式、Q4/Q8 量化等级（模型列表里的 "Q4_0" 字样）
3. 显存/内存估算：1.5b 模型 CPU 8GB 即可；7b 建议 16GB 或 8GB 显存
4. 服务接口：原生 API（`/api/tags`、`/api/generate`）与 OpenAI 兼容 API（`/v1/*`）
5. 模型注册：模型要写进项目 `model_settings.yaml` 才能被后端路由（本项目 Ollama 面板已实现自动注册）

**结合项目练习**

- 练习1：`ollama run qwen2:1.5b` 命令行对话，感受模型能力边界。
- 练习2：拉一个 7b 模型（如 `qwen2:7b`），在 WebUI"Ollama 模型"面板切换后对比回答质量。
- 练习3：阅读 `webui_pages/ollama_panel.py`，理解面板如何读取 `/api/tags`、如何把新模型写进配置。

**推荐资料**：Ollama 官网文档；Ollama 模型库（ollama.com/library）按显存挑选模型。

---

## 附：学习节奏建议（16 周参考）

| 周次 | 目标 |
|------|------|
| 1~3 | Python 基础收尾 + 项目跑起来（对照《项目部署与开发启动文档.md》） |
| 4~5 | Streamlit：能看懂并修改 webui_pages 下的界面代码 |
| 6~8 | Langchain：跑通对话链路，读懂 chat.py 主干 |
| 9~10 | RAG：完成一次自己的知识库上传+检索调参实验 |
| 11 | OpenAI 协议：抓包/打印流式响应，理解协议细节 |
| 12 | Ollama：完成本地模型部署与切换 |
| 13~14 | 综合小项目：自选工具接入 Agent 或自建知识库问答 |
| 15~16 | 复盘输出：写一篇"基于 Langchain-Chatchat 的大模型应用实践"技术报告 |

> 大数据专业衔接点：RAG 流水线本质是**数据管道**（抽取→转换→索引→查询），与数仓/ETL 思想一致；向量检索是**近邻搜索（ANN）**在大数据领域的典型应用。理解这两点，就能把大模型应用平滑接入你已有的知识体系。
