# 项目部署与开发启动文档

> 适用版本：Langchain-Chatchat 0.3.1.3（本仓库）
> 适用系统：Windows 10/11（文中命令均在本项目 Windows 环境验证通过）
> 文档目的：让新接手的同学从零开始，把本项目在本机跑起来并进入开发状态。

---

## 一、项目环境前置准备

### 1.1 硬件与网络要求

| 项目 | 最低要求 | 说明 |
|------|---------|------|
| 内存 | 8 GB | 运行 qwen2:1.5b 等小模型建议 8GB 以上 |
| 磁盘 | 20 GB 可用空间 | 包含依赖、模型权重、知识库向量数据 |
| 网络 | 可访问 PyPI / Ollama 官网 | 首次安装依赖和拉取模型需要联网 |

### 1.2 软件要求

1. **Python 3.10 ~ 3.11**（项目要求 `>=3.10,<3.12`，本项目 venv 实际使用 3.10.10）
   - 下载地址：https://www.python.org/downloads/
   - 安装时勾选 **Add Python to PATH**。
2. **Git**（拉取代码、提交修改用）
   - 下载地址：https://git-scm.com/download/win
3. **Ollama**（本地大模型运行时，见第四节）
   - 下载地址：https://ollama.com/download

### 1.3 获取项目代码

```powershell
git clone <你的仓库地址> Langchain-Chatchat
cd Langchain-Chatchat
```

---

## 二、虚拟环境创建与依赖安装

> 项目根目录已自带 `venv/` 虚拟环境。若你是全新拉取的代码（没有 venv 目录），按下面步骤创建。

### 2.1 创建虚拟环境

```powershell
# 在项目根目录执行
python -m venv venv
```

### 2.2 激活虚拟环境

```powershell
# PowerShell（推荐，首次使用如报执行策略错误，见第四节排查 Q1）
.\venv\Scripts\Activate.ps1

# 或 CMD
.\venv\Scripts\activate.bat
```

激活成功后命令行前会出现 `(venv)` 前缀。

### 2.3 安装依赖

本项目后端代码位于 `libs/chatchat-server`，以可编辑方式安装（改代码立即生效，无需重装）：

```powershell
# 升级 pip 后安装
python -m pip install -U pip

# 安装主程序（editable 模式，包含全部依赖）
pip install -e libs\chatchat-server
```

> 说明：依赖较多（langchain、faiss、torch 相关、streamlit 等），国内网络建议先切换清华镜像：
> ```powershell
> pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
> ```

### 2.4 验证安装

```powershell
chatchat --version   # 输出 0.3.1.3 即安装成功
```

### 2.5 初始化项目配置与知识库

```powershell
# 在 libs\chatchat-server 目录下执行（配置文件生成在当前目录）
cd libs\chatchat-server

# 生成 basic_settings.yaml / model_settings.yaml / kb_settings.yaml 等配置模板
chatchat init

# 初始化默认知识库（samples），不需要知识库功能可跳过
chatchat kb -r
```

---

## 三、本地开发启动 WebUI

### 3.1 一键启动（推荐：后端 API + WebUI 一起拉起）

```powershell
# 激活虚拟环境后，在 libs\chatchat-server 目录执行
cd libs\chatchat-server
chatchat start -a
```

启动成功后日志会出现：

```
INFO:     Uvicorn running on http://127.0.0.1:7861 (Press CTRL+C to quit)
You can now view your Streamlit app in your browser.
URL: http://127.0.0.1:8501
```

| 服务 | 地址 | 用途 |
|------|------|------|
| 后端 API Server | http://127.0.0.1:7861 | 对话、知识库、工具等全部接口 |
| WebUI (Streamlit) | http://127.0.0.1:8501 | 浏览器访问的前端界面 |

浏览器打开 **http://127.0.0.1:8501** 即可使用。

### 3.2 只启动 WebUI（前端开发模式）

只调 WebUI 页面样式/交互时，可以只启动前端：

```powershell
# 需在项目根目录执行（加载 .streamlit\config.toml 主题与端口配置）
cd Langchain-Chatchat
chatchat start -w
```

前端热重载开发方式（改代码自动刷新页面）：

```powershell
cd Langchain-Chatchat
.\venv\Scripts\streamlit.exe run libs\chatchat-server\chatchat\webui.py
```

> 注意：仅启动 WebUI 时对话/知识库功能不可用（缺少后端），但页面布局、上传校验提示等纯前端逻辑可以正常调试。

### 3.3 修改默认端口

- WebUI 端口：项目根目录 `.streamlit\config.toml` 中 `port = 8501`；
- API 端口：`libs\chatchat-server\basic_settings.yaml` 中 `API_SERVER.port = 7861`。

### 3.4 停止服务

在启动窗口按 `Ctrl + C`；或直接在任务管理器/PowerShell 结束相关 python 进程：

```powershell
Get-CimInstance Win32_Process -Filter "Name like '%python%'" |
  Where-Object { $_.CommandLine -match 'chatchat|streamlit' } |
  ForEach-Object { taskkill /F /T /PID $_.ProcessId }
```

---

## 四、Ollama 本地对接配置

本项目模型平台已配置为 **ollama 平台 + qwen2:1.5b 模型**，无需 GPU 也能运行。

### 4.1 安装并启动 Ollama

1. 下载安装 Ollama（Windows 版一键安装）；
2. 安装完成后 Ollama 会自动在后台运行，占用 **127.0.0.1:11434** 端口；
3. 验证：浏览器打开 http://127.0.0.1:11434 显示 "Ollama is running"。

### 4.2 拉取模型

```powershell
# 对话模型（约 1GB，CPU 可跑）
ollama pull qwen2:1.5b

# 向量模型（知识库/RAG 必需）
ollama pull quentinz/bge-large-zh-v1.5

# 查看已安装模型
ollama list
```

### 4.3 项目侧配置（model_settings.yaml）

打开 `libs\chatchat-server\model_settings.yaml`，确认 MODEL_PLATFORMS 中 ollama 平台如下（已配置好可跳过）：

```yaml
MODEL_PLATFORMS:
  - platform_name: ollama
    platform_type: ollama
    api_base_url: http://127.0.0.1:11434/v1   # openai 兼容地址
    api_key: EMPTY
    api_concurrencies: 5
    auto_detect_model: false
    llm_models:
      - qwen2:1.5b          # 对话模型
    embed_models:
      - quentinz/bge-large-zh-v1.5   # 向量模型
```

> WebUI 侧边栏的"Ollama 模型"标签页可以在线读取本地模型列表并一键切换，切换后无需改配置文件（面板会自动同步写入配置并热更新）。

### 4.4 重启服务生效

```powershell
chatchat start -a
```

进入 WebUI → 右下角齿轮（模型配置）确认默认模型为 `qwen2:1.5b`，发一条消息有回复即对接成功。

---

## 五、常见启动报错故障排查清单

| 编号 | 现象 | 原因 | 解决办法 |
|------|------|------|----------|
| Q1 | PowerShell 提示 "在此系统上禁止运行脚本" | 系统执行策略限制 | 管理员 PowerShell 执行：`Set-ExecutionPolicy -Scope CurrentUser RemoteSigned`，或改用 CMD 激活 venv |
| Q2 | `pip install -e libs\chatchat-server` 报依赖冲突/超时 | 网络问题或 pip 版本过旧 | 先 `pip install -U pip`；配置清华镜像（见 2.3）；重试 |
| Q3 | WebUI 打不开（8501 端口无响应） | 服务未启动/端口被占/仅启动了 api | 确认用 `chatchat start -a` 启动；`netstat -ano \| findstr 8501` 查占用并结束占用进程；改 `.streamlit\config.toml` 端口 |
| Q4 | 对话报 "无法连接API服务器" | 后端 7861 未启动或被杀毒软件拦截 | 先确认 Uvicorn 日志出现在 7861；重试 `chatchat start -a`；检查防火墙 |
| Q5 | 对话报模型错误 / 一直无回复 | Ollama 未启动或模型未拉取 | `ollama list` 确认模型存在；`ollama run qwen2:1.5b` 手动测试；确认 11434 端口在监听 |
| Q6 | 知识库对话报 embedding 模型错误 | 向量模型未配置或未拉取 | 确认 `model_settings.yaml` 中 embed_models 已配置并 `ollama pull quentinz/bge-large-zh-v1.5` |
| Q7 | `chatchat init` 后启动仍读旧配置 | 配置文件在工作目录之外 | chatchat 以**当前目录**找配置，务必在 `libs\chatchat-server` 目录下启动 |
| Q8 | 上传文件无响应/失败提示"无法连接后端服务" | 后端服务离线 | 检查 7861 是否存活（见 Q4）；后端恢复后重新点击上传 |
| Q9 | Streamlit 页面空白或组件报错 | 浏览器缓存了旧前端 | Ctrl+F5 强制刷新；确认 venv 中 streamlit 版本为 1.34.0 |
| Q10 | 中文乱码（PowerShell 日志） | 终端编码非 UTF-8 | 执行 `chcp 65001` 后再启动服务（仅影响日志显示，不影响功能） |

---

## 六、启动自检清单（Checklist）

- [ ] `python --version` 在 3.10 ~ 3.11 之间
- [ ] venv 已激活（命令行有 `(venv)` 前缀）
- [ ] `chatchat --version` 输出 0.3.1.3
- [ ] Ollama 已运行：http://127.0.0.1:11434 可访问
- [ ] `ollama list` 中存在 qwen2:1.5b 与 quentinz/bge-large-zh-v1.5
- [ ] 在 `libs\chatchat-server` 目录执行 `chatchat start -a`
- [ ] http://127.0.0.1:7861/docs 能打开（后端正常）
- [ ] http://127.0.0.1:8501 能打开（WebUI 正常）
- [ ] 发送一条消息能收到回复（模型链路正常）
